# Phase 1 Context: Foundation Contracts

## Objective

Import the existing baseline into GSD and preserve the Vite React TypeScript Tailwind and contracts foundation.

## Canonical References

- `docs/AGENTS.md`
- `docs/contracts.ts`
- `docs/MVP_TASK.md`
- `package.json`
- `src/lib/contracts.ts`

## Imported Notes

This phase was already partially implemented before GSD initialization. GSD should treat it as historical/brownfield context and avoid redoing broad scaffolding unless a concrete verification failure requires it.

## Deferred Scope

- Do not use Phase 1 as permission for broad refactors.
