# Context

The repository is already a Vite app with React 19, TypeScript, Tailwind, Zod, and tests for the LLM client and search criteria. The docs include a strong product/architecture contract and an implementation-focused UI spec.

Root `ROADMAP.md` uses sprint names S1-S9; GSD `.planning/ROADMAP.md` converts these into Phase 1-9 while preserving order and current position.
